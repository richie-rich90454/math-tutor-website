// MathTutor AI - Legacy client configuration.
// The legacy client is served from the same origin as the backend
// so that IE6 can use cookies without CORS support.
// Override API_BASE_URL before this script loads if the backend
// is hosted separately from the legacy static files.
var API_BASE_URL = window.API_BASE_URL || "";
