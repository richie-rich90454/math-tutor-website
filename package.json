{
    "name": "math-tutor-website",
    "version": "2.0.0",
    "private": true,
    "scripts": {
        "dev:backend": "cd backend && mvn spring-boot:run",
        "dev:frontend": "cd frontend-modern && npm run dev",
        "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
        "build:modern": "cd frontend-modern && npm run build && node ../copy-legacy.js",
        "build:backend": "cd backend && mvn clean package -DskipTests",
        "build": "npm run build:modern && npm run build:backend",
        "start": "java -jar backend/target/math-tutor-backend-*.jar",
        "format:modern": "cd frontend-modern && npm run format",
        "typecheck": "cd frontend-modern && npx tsc --noEmit"
    },
    "devDependencies": {
        "concurrently": "^10.0.4"
    }
}
